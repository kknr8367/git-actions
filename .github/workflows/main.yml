name: git-actions

on:
  push:
    branches: 
     - dev
     - release/*
  issues:
    types:
      - opened
      - labeled
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: task
        run: echo "sample"
